#
# Copyright (c) 2026 Metaform Systems, Inc.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License, Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
#
# Contributors:
#      Metaform Systems, Inc. - initial API and implementation
#

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-proxy
  namespace: edc-v
  labels:
    app: auth-proxy
    platform: edcv
    type: edcv-infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-proxy
  template:
    metadata:
      labels:
        app: auth-proxy
        platform: edcv
        type: edcv-infra
    spec:
      containers:
        - name: auth-proxy
          image: ghcr.io/metaform/jad/auth-proxy:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: TOKEN_INTROSPECTION_URL
              value: http://keycloak.edc-v.svc.cluster.local:8080/realms/edcv/protocol/openid-connect/token/introspect
            - name: INTROSPECT_CLIENT_ID
              value: auth-proxy
            - name: INTROSPECT_CLIENT_SECRET
              value: auth-proxy-secret
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 3
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: auth-proxy
  namespace: edc-v
  labels:
    tier: infrastructure
spec:
  selector:
    app: auth-proxy
  ports:
    - port: 8080
      targetPort: 8080
      name: http
